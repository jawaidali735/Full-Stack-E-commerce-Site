/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},   // TailwindCSS Plugin
    autoprefixer: {},  // Autoprefixer Plugin (Make sure this is included)
  },
};

export default config;
